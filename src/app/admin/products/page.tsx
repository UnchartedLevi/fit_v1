import { requireAdmin } from "@/lib/auth"; import { demoProducts, money } from "@/lib/products";
export default async function Products(){const auth=await requireAdmin();let products=demoProducts;if(auth){const {data}=await auth.supabase.from("products").select("*").order("created_at",{ascending:false});if(data?.length)products=data}return <><span className="eyebrow">CATALOGUE</span><h1>Products</h1><p>Add and edit products through Supabase Studio; the RLS policies restrict writes to admins. A streamlined inline editor can be connected to the same table.</p><div className="table-wrap"><table><thead><tr><th>Name</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.category}</td><td>{money(p.price)}</td><td>{p.stock_quantity}</td><td><span className="badge">{p.is_active?"Active":"Hidden"}</span></td></tr>)}</tbody></table></div></>}
