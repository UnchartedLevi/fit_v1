"use client";
import { FormEvent, useState } from "react"; import Link from "next/link"; import { createClient } from "@/lib/supabase/client"; import { toast } from "sonner"; import { useRouter } from "next/navigation";
export function AuthForm({mode}:{mode:"login"|"signup"}){const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const supabase=createClient();if(!supabase)return toast.error("Add Supabase variables to .env.local");setBusy(true);const f=new FormData(e.currentTarget),email=String(f.get("email")),password=String(f.get("password"));const {error}=mode==="login"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{data:{full_name:f.get("name")}}});setBusy(false);if(error)return toast.error(error.message);toast.success(mode==="login"?"Welcome back":"Check your email to confirm your account");router.push(mode==="login"?"/":"/auth/login");router.refresh()}return <form className="auth-card" onSubmit={submit}><span className="eyebrow">FITS ACCOUNT</span><h1>{mode==="login"?"Sign in":"Join FITS"}</h1>{mode==="signup"&&<label className="field"><span>Full name</span><input name="name" required/></label>}<label className="field"><span>Email</span><input type="email" name="email" required/></label><label className="field"><span>Password</span><input type="password" name="password" minLength={8} required/></label><button className="add-button" disabled={busy}>{busy?"Please wait…":mode==="login"?"Sign in":"Create account"}</button><p className="auth-note">{mode==="login"?<>New here? <Link href="/auth/signup">Create an account</Link></>:<>Already registered? <Link href="/auth/login">Sign in</Link></>}</p></form>}

