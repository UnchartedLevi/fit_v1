"use client";
import { useState } from "react"; import { Product } from "@/lib/types"; import { money } from "@/lib/products"; import { ProductVisual } from "./product-visual"; import { useCart } from "./cart-provider";
export function ProductDetail({product}:{product:Product}){const [size,setSize]=useState(product.sizes[0]),[qty,setQty]=useState(1);const {add}=useCart();return <div className="detail"><ProductVisual name={product.name} image={product.images[0]}/><div className="detail-info"><span className="eyebrow">{product.category} / FITS</span><h1>{product.name}</h1><p className="price">{money(product.price)}</p><p className="description">{product.description}</p><b>SELECT SIZE</b><div className="sizes">{product.sizes.map(x=><button className={`size ${size===x?"active":""}`} onClick={()=>setSize(x)} key={x}>{x}</button>)}</div><div className="qty"><button onClick={()=>setQty(Math.max(1,qty-1))}>−</button><span>{qty}</span><button onClick={()=>setQty(Math.min(product.stock_quantity,qty+1))}>+</button></div><button className="add-button" onClick={()=>add(product,size,qty)}>Add to bag — {money(product.price*qty)}</button><p className="stock">{product.stock_quantity} available · Secure checkout · Lagos delivery</p></div></div>}

